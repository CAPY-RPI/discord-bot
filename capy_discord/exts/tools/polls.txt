import logging
import discord
from discord import app_commands
from discord.ext import commands

from capy_discord.ui.embeds import info_embed

class Poll(commands.Cog):
    def __innit__(self, bot: commands.Bot) -> None:
        self.bot = bot
        self.log = logging.getLogger(__name__)
        
    @app_commands.command(name="poll", description="create a poll with reaction votes")
    @app_commands.describe(
        question="Whats the poll about",
        options="Optional: Comma-separated list of options (e.g. Yes, No, Maybe)",
    )
    
    async def poll